/* ───────── PianoSpark Desktop – storage.js ───────── */
/* Desktop storage bridge — placeholder for future appData migration */
/* For now, browser localStorage persists. This module provides the bridge plan. */

// Future: move profile data, settings, backups, imported libraries,
// cached charts from localStorage to JSON files under app.getPath('userData').
module.exports = {};
