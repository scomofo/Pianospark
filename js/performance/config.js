(function(){
  var PERFORMANCE_CONFIG = {
    highway: {
      scrollSpeed: 180,
      lookaheadSec: 3.0,
      hitLineTopPx: 300
    },
    ui: {
      hitBadgeMs: 1200
    }
  };
  window.PERFORMANCE_CONFIG = PERFORMANCE_CONFIG;
})();
